# 复制成 backend/.env 后修改（.env 不进 git，服务器上需要手动创建）
#
# 这里故意不设 DATABASE_URL：
#   - 本地开发时 app/config.py 默认就是 sqlite:///./app.db
#   - 容器里由 docker-compose 的 environment 注入绝对路径 sqlite:////app/data/app.db
# 一旦在这个文件里写死路径，就得依赖 Compose 的优先级规则才能不出错，没必要冒这个险。

# 签发登录令牌用，生产环境必须换成随机串，例如: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production

# 后台 /admin 的登录密码
ADMIN_PASSWORD=change-me
